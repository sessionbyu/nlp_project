# Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# 数据库
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0

# VADER 情感分析
nltk>=3.8.0

# 文件监控
watchdog>=4.0.0

# PyTorch 和相关包
# 指定额外的索引源，而不是覆盖主索引
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.1.0 # 或者 torch cpu 版本
transformers>=4.35.0
sentencepiece>=0.1.99

# 其他常用依赖
python-multipart>=0.0.6
python-dotenv>=1.0.0
# ==============================
# 新增依赖（第三阶段扩展）
# ==============================

# JWT 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Redis 缓存
# redis==5.0.1
# 改为
redis==4.6.0  # 或 4.5.5 以上但低于 5.0.0
# redis-py-cluster removed due to incompatibility with redis 5.x
# If needed, use redis>=4.5.0,<5.0.0 for cluster support

# 数据导出
pandas==2.0.3
openpyxl==3.1.2
numpy>=1.21.0,<2.0.0

# 异步任务（可选）
# celery==5.3.4
# celery[redis]==5.3.4

# WebSocket
# python-socketio==5.10.0

# 监控
prometheus-client==0.19.0
psutil>=5.9.0
sentry-sdk==1.39.1

# Celery 异步任务
celery==5.3.4
celery[redis]==5.3.4

# WebSocket
python-socketio==5.10.0

# 邮件发送（可选）
# aiosmtplib==2.0.2
email-validator==2.1.0

# 文件处理
# python-magic==0.4.27
# Pillow==10.1.0

# 文本处理
# jieba==0.42.1
# sumy==0.11.0

# 数据验证增强
# email-validator==2.1.0
